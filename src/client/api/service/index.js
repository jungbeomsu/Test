import {setDispatch} from "./process";
import {setting} from "./setting";
import {user} from "./user";
import {room} from "./room";

const serviceApi = {
  setDispatch: setDispatch,
  setting,
  user,
  room
}

export default serviceApi;
